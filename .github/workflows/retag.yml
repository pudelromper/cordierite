# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json

# WARNING: This workflow is meant for emergency use only.
# Under NO CIRCUMSTANCES should this workflow be automatically triggered.
name: Retag image

env:
  REPO_OWNER: ${{ github.repository_owner }}

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: true

permissions: {}

on:
  workflow_dispatch:
    inputs:
      old_tag:
        description: "Old tag from which images will be retagged to latest"
        required: true
        type: string

jobs:
  safeguard-timer:
    runs-on: ubuntu-latest
    if: inputs.old_tag != ''
    steps:
      - name: Safeguard wait
        env:
          SLEEP_TIME: "30"
        run: |
          echo "Triggered safeguard."
          echo "You have $SLEEP_TIME seconds to cancel the workflow if you want to cancel retagging."
          sleep $SLEEP_TIME

  retag-images:
    runs-on: ubuntu-latest
    if: inputs.old_tag != ''
    needs: [safeguard-timer]
    permissions:
      packages: write
    steps:
      # The image registry name must be lower-case, but GitHub usernames can be capitalized.
      - name: Get lower-case equivalent of the repository owner's username
        id: repo_owner_case
        run: echo "lowercase=${REPO_OWNER,,}" >> "$GITHUB_OUTPUT"

      - uses: Zeglius/ghcr-oci-retagger@1e08f1dac27d25efe1377d9a35f133731bf3d986
        env:
          ver: "44"
        with:
          GITHUB_TOKEN: ${{ github.token }}
          PREFIX: ghcr.io/${{ steps.repo_owner_case.outputs.lowercase }}/
          TAG_MAPPINGS: |-
            cordierite:${{ inputs.old_tag }}                        => cordierite:latest,stable,${{ env.ver }}
            cordierite-deck:${{ inputs.old_tag }}                   => cordierite-deck:latest,stable,${{ env.ver }}
            cordierite-nvidia:${{ inputs.old_tag }}                 => cordierite-nvidia:latest,stable,${{ env.ver }}
            cordierite-nvidia-open:${{ inputs.old_tag }}            => cordierite-nvidia-open:latest,stable,${{ env.ver }}
